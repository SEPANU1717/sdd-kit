{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/sepanu-sdd/review.schema.json",
  "allOf": [{ "$ref": "common.schema.json" }, { "properties": { "artifact": { "const": "review" }, "status": { "enum": ["pass", "changes-requested", "blocked"] }, "risk": { "enum": ["quick", "standard", "high-risk"] } } }]
}
