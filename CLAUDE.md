@AGENTS.md

Claude Code note: skills are mirrored into `.claude/skills/` so they appear as
slash commands (`/sdd-specify`, `/sdd-plan`, `/sdd-implement`, `/sdd-review`, `/sdd-ship`,
`/sdd-status`). Edit them in `.agents/skills/`, then run `scripts/sync-skills.ps1`
(Windows) or `scripts/sync-skills.sh` to update the mirror.
